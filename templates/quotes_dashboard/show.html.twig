{% extends 'base.html.twig' %}

{% block title %}Quotes{% endblock %}

{% block body %}
    <h1>Quotes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quote.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ quote.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quotes_dashboard_index') }}">back to list</a>

    <a href="{{ path('quotes_dashboard_edit', {'id': quote.id}) }}">edit</a>

    {{ include('quotes_dashboard/_delete_form.html.twig') }}
{% endblock %}
